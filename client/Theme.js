import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    bio: {
      fontSize: 12,
      maxWidth: 150,
      textAlign: 'center',
    },
  },
});

export default theme;
